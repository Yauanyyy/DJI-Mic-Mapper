请帮我写一个非常轻量的 Windows 11 后台程序，把 DJI Mic Mini 发射器上的按钮映射成可自定义的键盘按键（比如F13）。
已知设备信息：
VID = 0x2CA3
PID = 0x4011
已确认，短按 Mic Mini 配对按钮时会产生：
HID Usage Page = 0x25F0
Usage = 0x0100
并且有明确的按下和松开事件。
需求：
1. 只做 Toggle。
2. 每次 DJI Mic Mini 按钮按下时，只模拟一次目标按键；松开时不再次触发。
3. 一次物理点击只能触发一次，长按或重复 HID report 不能连续触发。
4. 必须根据 VID/PID 和 HID Usage 判断设备来源，不影响普通键盘、耳机等其他设备。
5. 优先监听：
    VID 0x2CA3
    PID 0x4011
    Usage Page 0x25F0
    Usage 0x0100
6. 使用 Windows 原生 Raw Input / HID API 和 SendInput，事件驱动，不要轮询。
7. 程序尽量轻量，简单 GUI，后台运行即可。
8. 支持设备热插拔。
9. 不修改 DJI Mic Mini 驱动，不影响 WinUSB Interface 6、DJI Mic Control 和麦克风音频。
10. 需要同时屏蔽 DJI Mic Mini 按钮原本产生的 Volume Up。
目标按键必须可修改，不要写死为 某个案件
可以使用一个简单配置文件，例如 config.toml：
target = "F13"

程序启动时读取配置，并根据配置通过 SendInput 模拟对应按键/快捷键。
可以使用 Rust，优先使用：
RegisterRawInputDevices
WM_INPUT
GetRawInputData
GetRawInputDeviceInfo
SendInput
请先做最小可用版本，不要加入复杂 GUI 或其他不必要功能。